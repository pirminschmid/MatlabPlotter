language: cpp

compiler:
  - gcc
  - clang

before_install:
  - echo $LANG
  - echo $LC_ALL
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get update && sudo apt-get install -y libeigen3-dev; fi
  - cd testing

script:
  - ./build_examples.sh

after_success:
  - ./run_examples.sh

branches:
  only:
    - master

notifications:
  email:
    - "mailbox@pirmin-schmid.ch"

os:
  - linux